# Plantilla de variables de entorno
# Copiar este archivo a .env.local y completar los valores según necesario

# Configuración de la app (opcional)
NEXT_PUBLIC_APP_NAME=My TS Project
NEXT_PUBLIC_APP_VERSION=1.0.0

# Variables privadas del servidor (solo en node)
# (Agregar según sea necesario)
